# Server Management & Audit Tool 🚀

ابزار جامع مدیریت و نظارت بر سرور با قابلیت‌های امنیتی پیشرفته

## ویژگی‌های اصلی ✨

### 1. نمایش اطلاعات سرور 📊
- نمایش گرافیکی و زیبای مشخصات سیستم
- اطلاعات CPU، RAM، Disk
- آدرس‌های IP (IPv4/IPv6)
- وضعیت شبکه و اینترفیس‌ها

### 2. بررسی وضعیت IP 🔍
- چک کردن تمیز یا کثیف بودن IP
- بررسی Fraud Score
- تشخیص Proxy/VPN
- چک DNS Blacklist
- بررسی DNS Leak
- اطلاعات Geolocation و ISP

### 3. MTU Finder & Optimizer 📡
- تشخیص خودکار بهترین MTU
- تست MTU سفارشی
- تنظیم خودکار و دائمی MTU
- پشتیبانی از IPv4 و IPv6

### 4. Security Audit 🔒
- بررسی تنظیمات SSH
- وضعیت فایروال
- چک Fail2ban/CrowdSec
- بررسی Login های ناموفق
- آپدیت‌های امنیتی
- پورت‌های باز
- SUID Files مشکوک
- Password Policy

### 5. Network Diagnostics 🌐
- تست DNS Resolution
- Ping و Latency Test
- Traceroute
- Speed Test
- بررسی کانکشن

### 6. Log Analysis 📋
- تحلیل خطاهای سیستم
- بررسی لاگ‌های Authentication
- Disk I/O Errors
- آخرین رویدادهای مهم

## نصب و راه‌اندازی 💻

### دانلود از GitHub:
```bash
wget https://raw.githubusercontent.com/YOUR_USERNAME/server-tool/main/server
chmod +x server
sudo mv server /usr/local/bin/
```

### یا نصب مستقیم:
```bash
curl -fsSL https://raw.githubusercontent.com/YOUR_USERNAME/server-tool/main/server | sudo bash
```

### نصب دستی:
1. فایل `server` را دانلود کنید
2. اجرای دستورات زیر:
```bash
chmod +x server
sudo mv server /usr/local/bin/
```

## استفاده 🎯

### اجرای ساده:
```bash
sudo server
```

### دستورات موجود در منو:
- `1` - بررسی وضعیت IP و DNS Leak
- `2` - MTU Finder و بهینه‌سازی
- `3` - Security Audit کامل
- `4` - تست‌های شبکه
- `5` - تحلیل لاگ‌ها
- `6` - گزارش کامل (همه تست‌ها)
- `7` - مشاهده آخرین گزارش
- `0` - خروج

## سیستم‌عامل‌های پشتیبانی شده 🐧

✅ Ubuntu 18.04, 20.04, 22.04, 24.04
✅ Debian 9, 10, 11, 12
✅ CentOS 7, 8, 9
✅ RHEL 7, 8, 9
✅ Fedora

## پیش‌نیازها 📦

اسکریپت به صورت خودکار این پکیج‌ها را نصب می‌کند:
- curl
- wget
- jq
- bc
- net-tools
- dnsutils
- traceroute

## گزارش‌ها 📄

همه گزارش‌ها در فایل‌های زیر ذخیره می‌شوند:
```
server-report-YYYYMMDD_HHMMSS.txt
```

## امنیت 🔐

- اسکریپت باید با دسترسی root اجرا شود
- هیچ اطلاعات حساسی ذخیره نمی‌شود
- همه تغییرات از فایل‌های اصلی backup می‌گیرند

## مثال‌های استفاده 💡

### چک کردن وضعیت IP:
```bash
sudo server
# انتخاب گزینه 1
```

### تنظیم MTU بهینه:
```bash
sudo server
# انتخاب گزینه 2
# انتخاب Auto-detect
```

### گزارش امنیتی کامل:
```bash
sudo server
# انتخاب گزینه 6
```

## توضیحات تکمیلی 📚

### IP Reputation Score:
- **80-100%**: IP تمیز است ✅
- **60-79%**: IP متوسط است ⚠️
- **0-59%**: IP مشکل دارد ❌

### Security Audit Status:
- **PASS**: همه چیز خوب است ✅
- **WARN**: نیاز به بررسی دارد ⚠️
- **FAIL**: مشکل امنیتی جدی ❌

### MTU Optimization:
- MTU پیش‌فرض معمولاً 1500 است
- برای بهترین عملکرد، MTU بهینه را پیدا کنید
- اسکریپت به صورت خودکار بهترین مقدار را تشخیص می‌دهد

## عیب‌یابی 🔧

### اگر اسکریپت اجرا نشد:
```bash
sudo chmod +x /usr/local/bin/server
```

### اگر خطای permission داد:
```bash
sudo server
```

### اگر dependency ها نصب نشدند:
```bash
# Ubuntu/Debian:
sudo apt-get update
sudo apt-get install -y curl wget jq bc net-tools dnsutils traceroute

# CentOS/RHEL:
sudo yum install -y curl wget jq bc net-tools bind-utils traceroute
```

## Features در نسخه‌های بعدی 🚀

- [ ] Auto-fix برای مشکلات امنیتی
- [ ] Email notification برای مشکلات
- [ ] Dashboard وب‌سایت
- [ ] Monitoring پیوسته
- [ ] Integration با Telegram Bot

## مشارکت 🤝

برای گزارش مشکلات یا پیشنهادات:
- Issue ثبت کنید
- Pull Request بفرستید

## لایسنس 📜

MIT License - استفاده آزاد برای همه

## نویسنده ✍️

نسخه: 1.0.0
تاریخ: 2024

---

**نکته مهم**: این ابزار فقط برای نظارت و بهبود امنیت سرور است. هیچ تغییری بدون تایید شما اعمال نمی‌شود.

**توجه**: برای استفاده در Production، حتماً ابتدا در محیط Test آزمایش کنید.
